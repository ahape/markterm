# Markterm Dependencies
#
# For pip users who prefer requirements.txt:
# This file is kept for backward compatibility, but we recommend using pyproject.toml
#
# Installation:
#   pip install -e .                    # Install package
#   pip install -e ".[dev]"             # Install with development dependencies
#
# Or directly from requirements.txt:
#   pip install -r requirements.txt

rich>=14.0.0
markdown-it-py>=4.0.0
Pygments>=2.18.0
